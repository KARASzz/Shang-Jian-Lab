# 按子智能体规则实施工作台

## 1. 调度与复核

已实际创建一个子智能体完成流水线静态审查，创建参数为 `gpt-5.6-luna`、`max`；实际后端模型身份未独立核实。主线程核对了现有存档：共 13 个文件，含隐藏文件，尚未修改或迁移。

当前仍为计划模式，实施按以下分工执行。所有执行子智能体均显式指定上述模型和推理强度，不得自行再委派。

| 执行任务 | 允许修改范围 | 交付与验收 |
|---|---|---|
| 主线程：基础约定 | 根目录规则、配置样例、共享数据类型、存档目录 | 固定模块接口；存档迁移前后哈希一致 |
| 子智能体 A：模型与搜索 | `工作台/接入/` 及对应测试 | 统一模型接口、两路 MCP、Bing 公开搜索；覆盖连接失败和降级 |
| 子智能体 B：内容流水线 | `工作台/流水线/`、`提示词/` 及对应测试 | 策划、三稿、审稿、返修、恢复流程可运行 |
| 子智能体 C：数字工作台 | `工作台/菜单/`、双平台启动脚本及对应测试 | 数字菜单、配置、任务浏览、定稿和归档入口 |
| 主线程：集成交付 | 集成入口、文档及必要修复 | 检查全部差异，执行端到端验收，失败则安排原负责人返工 |

先由主线程固定共享接口，再并行委派 A、B、C；各自使用接口桩验证，集成由主线程串行完成。每次委派明确输入、文件范围、依赖和验收，结果必须报告实际修改、验证和遗留问题。

## 2. 产品与目录

采用 Python 本地编排，Windows `.bat`、macOS `.command` 启动，共用数字菜单。

```text
根目录/
├── AGENTS.md
├── README.md
├── 启动工作台.bat
├── 启动工作台.command
├── 工作台/
├── 配置/
├── 提示词/
├── 进行中/
│   └── 时间戳-专栏/
│       ├── 选题/
│       ├── 资料/
│       ├── 策划/
│       ├── 三篇初稿/
│       ├── 审稿与返修/
│       ├── 待定稿/
│       └── 运行记录/
└── 存档/
    └── 2026-09-09-14-34-16/
```

现有一期整体迁入存档，保留正文、文件名、隐藏目录和配图。外部参考归属不变；历史稿仅作风格参考，不作为新稿事实依据。

主菜单固定为：`1 新建一期、2 继续任务、3 查看稿件、4 重跑步骤、5 导入手改稿／定稿、6 归档、7 历史存档、8 配置、9 诊断、0 退出`。

三个专栏为“大模型二三事”“AI风险治理与审计”“开源项目”。默认中文公众号稿，约 3000 字；首版不包含自动发布、配图或网页生成。

## 3. 三模型流水线

三个模型共用一个 OpenAI 兼容 Base URL 和密钥配置，采用 Chat Completions：

1. 用户选择专栏，Tavily / Brave / Bing 各检索两轮并由 Scrapy 抓取正文；研究完成后 MiniMax 提出 5 个候选选题，用户数字选题或输入自选题。
2. `minimax-m3` 生成检索任务；程序调用 Tavily MCP、Brave MCP 和 Bing 公开搜索，并由 Scrapy 抓取选题后的来源正文，MiniMax 整理证据。
3. MiniMax 为同一选题策划三个不同角度，分别明确中心判断、结构和证据。
4. `qwen3.7-plus` 分三次独立调用生成三篇完整稿件，默认顺序执行。
5. `glm-5.1` 独立审查三篇，输出问题清单、评分和推荐稿。
6. Qwen 返修推荐稿，GLM 复审；必要时补证，最多自动返修两轮。
7. 输出推荐待定稿、备选标题、摘要及资料口径，保留全部初稿和审稿记录。
8. 用户导入手改稿、确认定稿，再整期归档。自动流程不得覆盖用户定稿。

专栏审稿分别关注：模型能力与宣传口径；风险措施、法域和标准版本；开源许可证、维护状态和实测边界。

## 4. 共享接口与失败处理

采纳子智能体审查中关于搜索接口、结构化审稿和版本恢复的建议；原计划已有的岗位固定及禁止静默换模型继续保留。

- **模型接口：**统一返回正文、请求模型、服务端返回模型、用量及错误。配置快照固定本期岗位；返回模型别名差异记入诊断，不仅凭字符串差异认定调用了错误模型。
- **搜索接口：**统一输出来源编号、URL、标题、发布与访问日期、正文、摘录定位、获取状态。MCP 配置明确传输方式、启动命令或地址、凭据引用和工具映射，并执行初始化与工具发现。
- **证据约束：**关键事实绑定证据编号；搜索摘要仅用于发现原文。无法取证、PDF 无法提取或渠道受限时明确记录，不编造补齐。
- **审稿接口：**包含稿件版本、问题、严重程度、证据定位、推荐及是否通过。虚构引用、关键事实无依据、样本越界和假个人经历必须阻断，评分不能抵消。
- **任务状态：**阶段完成后原子保存检查点。重跑上游生成新版本并将下游标为过期，恢复时禁止混用版本；两轮返修后仍不通过则进入“需人工处理”。
- **调用限制：**深入检索最多两轮、30 个去重来源、站内深度两层；模型超时默认 180 秒，网络错误和限流最多重试两次，认证失败直接停止。
- **凭据保护：**本地隐藏输入、日志脱敏、Git 忽略。网页内容只能作为资料，不能变成执行指令。

## 5. 验收与完成条件

主线程必须验证：

- 存档迁移前后文件清单和哈希一致，定稿及外部参考未改写。
- 两个平台的数字菜单能处理中文与空格路径；缺少真实 Windows 环境时明确标注未实测。
- 三个指定模型分别真实参与策划、三篇写稿及审稿；两路 MCP 和公开搜索分别完成真实连接验证。
- 假引用、渠道失败、验证码、断点恢复、上游重跑、返修超限及定稿覆盖保护均有针对性测试。
- 至少完成一期真实端到端流程，另两个专栏以固定资料验证专属要求。
- 子智能体结果由主线程检查差异并复核，不能仅凭子智能体自述宣布完成。

接口地址、凭据和 MCP 参数在首次配置时填写。当前没有执行模型服务调用、文件修改或存档迁移；实施需在会话退出计划模式后开始。
