# 存档/

历史期目录由 6 号菜单「归档」从 `进行中/<issue>/` 整体迁入。归档前要求：

1. 用户已在 5 号菜单导入定稿（带书名号的 `熵减进化室-公众号成稿-《…》.md`）。
2. `运行记录/` 中所有阶段标记为已结束。
3. 配置文件、密钥、`.workbuddy/memory/` 不随期迁移；只保留当期可复现产物。

归档后**只读**。如需修订，应新建一期重做，而非修改存档。